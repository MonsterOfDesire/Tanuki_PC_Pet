# 玩家更新公告：從 0.3.0 到目前版本

## 先說一聲

`0.3.0` 版本的程式其實還有保留，位置在：

- `archive/legacy_versions/0.3.0.py`

這次的更新說明，就是以那個版本為起點，和目前的 `lab_2.py` 與 `tanuki_core/` 架構做對照整理。

## 給玩家的更新重點

這一版不只是修 bug，而是把狸貓桌寵從「主要待在桌面上的桌寵」，慢慢擴充成更完整的桌面陪伴系統。

現在的馬娘們，除了原本在工作列附近活動之外，也能：

- 停留在應用程式視窗上方
- 跟著視窗一起移動
- 在更多情境下做出更自然的互動與照護行為
- 記住你的部分設定，減少每次重開都要重新調整的麻煩

同時，我們也把很多原本藏在大型單檔裡的底層邏輯重新整理好了，讓後續可以更穩定地繼續追加功能。

## 本次新增或明顯強化的內容

### 1. 視窗互動正式加入

和 `0.3.0` 相比，現在的馬娘不再只侷限在桌面底部。

目前版本新增了完整的視窗停留機制：

- 可拖曳到應用程式視窗上方並吸附停留
- 停在視窗上的角色會跟著視窗移動
- 有飛行能力的角色，在特定條件下會自主飛到視窗上方
- 視窗關閉時，角色會解除停留並自然掉落

這讓整體桌寵的活動空間比以前豐富很多，也更有「真的在桌面陪伴你」的感覺。

### 2. 儀表板功能更完整

相較 `0.3.0` 時期比較偏基本控制，現在的儀表板多了不少實用功能：

- 時間流速切換：`1x / 2x / 4x / 8x`
- 顯示比例切換：`1x / 1.5x / 2x / 3x`
- Debug 顯示開關
- Config / Manifest 檢查按鈕
- 關閉前的「正在儲存設定...」提示

對平常使用來說更方便，對測試新功能時也更有效率。

### 3. 設定記憶功能上線

這一版開始，系統會正式保存部分使用者設定。

目前可記住的內容包含：

- 角色位置
- 角色顯示 / 隱藏狀態
- 儀表板開關
- 社交冷卻
- 時間流速
- 顯示比例
- debug 狀態

比起早期版本，每次啟動都要重新調整的情況已經少很多。

### 4. 互動、掉落與拖曳穩定度提升

這一版也修了很多實際玩起來會在意的細節：

- 拖曳時更穩定觸發對應動畫
- 掉落後的彈跳恢復正常
- 重摔後不再長時間卡在奇怪狀態
- 視窗吸附與邊界行為更自然
- 照護、互動、恢復流程之間的銜接更一致

這些調整可能不一定會第一眼注意到，但會直接影響整體操作手感。

### 5. 開發與除錯工具也一起變強了

雖然這部分比較偏開發面，但它其實對未來更新速度很重要。

目前版本新增了：

- 角色頭上的 debug overlay
- 目前動畫 / mood / state / movement anchor 顯示
- `config.json` 與 `manifest_edit.json` 的檢查能力
- 可統一控制速度的 `SimulationClock`

這代表之後遇到動畫抽選、平台判定、互動流程這類問題時，我們比較能快一點找到原因，也比較不容易把修正做壞。

## 架構上的大改動

這是這一版最重要、但最不容易直接從畫面看出來的地方。

`0.3.0` 時期的邏輯主要集中在大型單檔裡；現在則正式拆成 `tanuki_core/` 多模組架構。

目前核心模組包含：

- `asset_manager.py`
- `config_store.py`
- `dashboard_ui.py`
- `geometry.py`
- `runtime.py`
- `validation.py`
- `window_tracker.py`
- `pet_basics.py`
- `pet_windowing.py`
- `pet_social_care.py`

另外，`TanukiPet` 也不再是一個什麼都包的大類別，而是改成由多個 mixin 組成，職責更清楚。

簡單說，這次不只是「多加了一些功能」，而是整個專案已經從「大型單檔 Python」正式走向「可持續擴充的多模組架構」。

## 專案結構整理

這次也順便把開發環境整理了一遍：

- 主程式放在 `tanuki_app/`
- 虛擬環境獨立到專案外層 `.venv/`
- `build/`、`dist/` 移到專案根層
- 舊版腳本移入 `archive/legacy_versions/`

這樣之後在 PyCharm、Git、打包流程上都會比以前穩定很多。

## 一句話總結

如果要用一句話形容這次更新，我會這樣寫：

> 這一版讓狸貓桌寵從單純待在桌面底部的桌寵，逐步進化成能停留在視窗、記住設定、支援更多互動與更穩定開發流程的桌面陪伴系統。

## 建議的公告標題

- `桌面停留與系統重構更新`
- `視窗互動更新`
- `0.3.0 之後的大型整理與功能擴充`
